apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: booth-backend-pipeline
description: Booth Agent backend — asia-south1
serialPipeline:
  stages:
    - targetId: dev
    - targetId: staging
      strategy:
        standard:
          verify: false
    - targetId: prod
      strategy:
        standard:
          verify: false
